/**
 * Room types - Re-export from shared types
 */
export type {
	Room,
	CreateRoomDto,
	RoomAmenity,
	RoomImage,
	RoomReview,
} from '@/types';

export { RoomStatus, AmenityType } from '@/types/enums';
